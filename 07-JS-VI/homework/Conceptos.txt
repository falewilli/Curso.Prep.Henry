Funciones Callback: es la capacidad de pasar una función como argumento a otra función. Dado que en Javascript las funciones son objetos de primera clase, las mismas pueden ser utilizadas del mismo modo que lo hacemos con otros objetos, ya que son objetos en si mismos. Las funciones pueden ser guardadas como variables, pasadas como argumentos o creadas dentro de otras funciones, por citar algunos ejemplos.